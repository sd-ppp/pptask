{
  "name": "pptask",
  "projectType": "library",
  "sourceRoot": "libs/domain/pptask/core/src",
  "tags": [
    "layer:domain",
    "type:domain-core",
    "scope:domain",
    "platform:agnostic"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-script",
      "options": {
        "script": "build"
      }
    },
    "dev": {
      "executor": "nx:run-script",
      "options": {
        "script": "dev"
      }
    },
    "test": {
      "executor": "nx:run-script",
      "options": {
        "script": "test"
      }
    },
    "typecheck": {
      "executor": "nx:run-script",
      "options": {
        "script": "typecheck"
      }
    }
  }
}
