{
  "name": "pptask",
  "projectType": "library",
  "sourceRoot": "pptask/src",
  "tags": [
    "layer:domain",
    "type:domain-core",
    "scope:domain",
    "platform:agnostic"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-script",
      "options": {
        "script": "build"
      }
    },
    "test": {
      "executor": "nx:run-script",
      "options": { "script": "test" }
    },
    "test-browser": {
      "executor": "nx:run-script",
      "options": { "script": "test:browser" }
    },
    "test-integration": {
      "executor": "nx:run-script",
      "options": { "script": "test:integration" }
    },
    "typecheck": {
      "executor": "nx:run-script",
      "options": { "script": "typecheck" }
    }
  }
}
